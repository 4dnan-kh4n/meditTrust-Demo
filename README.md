# mediTrust hackathon prototype

This is a standalone static prototype: no backend, database, login, API key, or installation is required. It uses hardcoded Indian doctor and facility data to demonstrate public discovery, profile detail, feedback, correction, verification, and admin-management flows.

Open `index.html` in a browser to present it.
